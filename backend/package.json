{
  "name": "ai-interview-agent-backend",
  "version": "0.1.0",
  "private": true,
  "main": "dist/src/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/index.js",
    "lint": "echo \"No lint configured\"",
    "test": "npm run build && node dist/test/session-store.test.js && node dist/test/engine.test.js && node dist/test/evaluator.test.js && node dist/test/decision.test.js && node dist/test/api.test.js"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2",
    "@types/express": "^4.17.17",
    "@types/node": "^20.5.1"
  }
}
